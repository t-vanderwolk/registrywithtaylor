# Nursery Academy Image Needs

After wiring the remaining matching assets from `public/assets/nurserypath`, these are the nursery-specific image gaps that still do not have a strong, dedicated Academy match.

## Still Need To Get

- `smart-bassinet-lifestyle`
  A real smart-bassinet image for the smart-bassinet section in `Sleep Space Decisions`. The current section works, but it is still borrowing a broader nighttime nursery image instead of a true smart-bassinet context.

- `stroller-bassinet-lifestyle`
  A lifestyle image of a stroller bassinet in real use. The module currently uses a stroller image that supports the point, but a true bassinet-on-stroller photo would be cleaner.

- `dedicated-changing-table`
  A true dedicated changing-table image. The furniture system is strongest around dresser-top changing, but the category copy still references dedicated changing tables as a valid lane.

- `baby-proofing-hardware`
  Anchors, latches, outlet covers, and cord-management hardware in real nursery context. The current baby-proofing visuals are room-level, not hardware-level.

- `blind-cord-and-monitor-cable-safety`
  A cleaner nursery-specific image for cord management. This would sharpen the baby-proofing module and remove the need to imply that point through broader room imagery.

- `standard-bag-diaper-pail-lifestyle`
  A contextual nursery image for a standard-bag diaper-pail setup. Current diaper-pail coverage is solid, but the visual mix would be stronger with one more lifestyle angle.

- `non-wifi-video-monitor-lifestyle`
  A dedicated non-WiFi monitor image in room context. This would make the monitor submodule feel less dependent on product-forward shots.

## Intentionally Still Unused

- `image.png`
  This file does not match the current TMBC Academy visual language, so it was intentionally left out.
